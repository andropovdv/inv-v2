export enum RouterNames {
  LOGIN_ROUTE = "/login",
  REGISTRATION_ROUTE = "/registration",
  START_PAGE = "/app",
  NODE = "/",
  USERS_ROUTE = "/app/users",
  VENDORS_ROUTE = "/app/vendors",
}

export enum pagination {
  pageSize = 5,
}
